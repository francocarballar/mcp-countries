/**
 * @constant API_URL
 * @description URL of the API
 */
export const API_URL = 'https://restcountries.mcp-blockchain-metadata.workers.dev'

/**
 * @constant API_VERSION
 * @description Version of the API
 */
export const API_VERSION = 'v1'

/**
 * @constant API_URL_WITH_VERSION
 * @description URL of the API with the version
 */
export const API_URL_WITH_VERSION = `${API_URL}/${API_VERSION}`


/**
 * @constant API_ENDPOINTS  
 * @description Endpoints of the API
 */
export const API_ENDPOINTS = {
  ALL: '/all',
  BY_NAME: '/name',
  REGIONS: '/regions',
  BY_REGION: '/region',
}
